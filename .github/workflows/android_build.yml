name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk python3-pip wget ccache

      - name: Install buildozer
        run: |
          pip install --upgrade pip
          pip install buildozer cython virtualenv

      - name: Setup Android SDK automatically
        run: |
          export PATH="$PATH:/home/runner/.local/bin"
          buildozer android update || true

      - name: Build APK
        run: |
          export PATH="$PATH:/home/runner/.local/bin"
          buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: babita-ultimate-apk
          path: bin/*.apk            wget \
            ccache
          pip install --upgrade pip
          pip install buildozer cython virtualenv

      - name: Clean old build
        run: rm -rf .buildozer ~/.buildozer

      - name: Setup Android SDK
        run: |
          export SDK=/home/runner/android-sdk
          export ANDROID_HOME=$SDK
          export ANDROID_SDK_ROOT=$SDK

          mkdir -p $SDK/cmdline-tools
          cd $SDK/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-9477386_latest.zip
          mv cmdline-tools latest
          rm commandlinetools-linux-9477386_latest.zip

          export PATH=$SDK/cmdline-tools/latest/bin:$SDK/platform-tools:$PATH

          mkdir -p ~/.android
          touch ~/.android/repositories.cfg

          yes | sdkmanager --licenses || true
          sdkmanager "platform-tools" "platforms;android-33" "build-tools;33.0.0" "ndk;25.2.9519653"

          echo "ANDROID_SDK_ROOT=$SDK" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "PATH=$PATH" >> $GITHUB_ENV

      - name: Build APK
        run: |
          buildozer android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: babita-ultimate-apk
          path: bin/*.apk

      - name: Upload log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log          mkdir -p $SDK/cmdline-tools
          cd $SDK/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q *.zip
          mv cmdline-tools latest
          rm *.zip
          
          export PATH=$SDK/cmdline-tools/latest/bin:$SDK/platform-tools:$PATH
          
          # Accept all licenses automatically
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg
          echo "y" | sdkmanager --licenses || true
          
          # Force install build-tools 33.0.0 (not rc)
          sdkmanager "platform-tools" "platforms;android-33"
          sdkmanager "build-tools;33.0.0"
          sdkmanager "ndk;25.2.9519653"
          
          echo "ANDROID_SDK_ROOT=$SDK" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "PATH=$PATH" >> $GITHUB_ENV

      - name: Override build-tools version
        run: |
          mkdir -p ~/.buildozer/android/platform/android-sdk/build-tools
          ln -s $ANDROID_HOME/build-tools/33.0.0 ~/.buildozer/android/platform/android-sdk/build-tools/33.0.0

      - name: Build APK
        run: |
          buildozer android debug | tee build.log
          exit ${PIPESTATUS[0]}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: babita-ultimate-apk
          path: bin/*.apk
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log          mkdir -p $SDK/cmdline-tools
          cd $SDK/cmdline-tools
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q *.zip
          mv cmdline-tools latest
          rm *.zip
          
          export PATH=$SDK/cmdline-tools/latest/bin:$SDK/platform-tools:$PATH
          
          # Accept all licenses automatically
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg
          echo "y" | sdkmanager --licenses || true
          
          # 🔥 FORCE install build-tools 33.0.0 (not rc)
          sdkmanager "platform-tools" "platforms;android-33"
          sdkmanager "build-tools;33.0.0"
          sdkmanager "ndk;25.2.9519653"
          
          echo "ANDROID_SDK_ROOT=$SDK" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "PATH=$PATH" >> $GITHUB_ENV

      - name: 🔥 Override build-tools version
        run: |
          mkdir -p ~/.buildozer/android/platform/android-sdk/build-tools
          ln -s $ANDROID_HOME/build-tools/33.0.0 ~/.buildozer/android/platform/android-sdk/build-tools/33.0.0

      - name: Build APK
        run: |
          buildozer android debug | tee build.log
          exit ${PIPESTATUS[0]}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: babita-ultimate-apk
          path: bin/*.apk
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log          unzip -q *.zip
          mv cmdline-tools latest
          rm *.zip
          
          export PATH=$SDK/cmdline-tools/latest/bin:$SDK/platform-tools:$PATH
          
          # Accept all licenses
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg
          yes | sdkmanager --licenses || true
          
          # Install specific versions
          sdkmanager "platform-tools" "platforms;android-33"
          sdkmanager "build-tools;33.0.0"
          sdkmanager "ndk;25.2.9519653"
          
          # Set environment variables
          echo "ANDROID_SDK_ROOT=$SDK" >> $GITHUB_ENV
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "PATH=$PATH" >> $GITHUB_ENV

      - name: Build APK
        run: |
          buildozer android debug | tee build.log
          exit ${PIPESTATUS[0]}

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: babita-ultimate-apk
          path: bin/*.apk
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.log
